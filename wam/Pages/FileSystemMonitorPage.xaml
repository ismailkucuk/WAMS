<UserControl x:Class="wam.Pages.FileSystemMonitorPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:wam.Pages">
    <StackPanel Margin="20">
        <TextBlock Text="📁 İzlenecek Klasör" FontSize="16" Margin="0 0 0 10"/>

        <StackPanel Orientation="Horizontal" Margin="0 0 0 10">
            <TextBox x:Name="TxtPath" Width="400" Margin="0 0 10 0"/>
            <Button Content="Gözat..." Click="BtnBrowse_Click"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Margin="0 0 0 10">
            <Button Content="▶ İzlemeyi Başlat" Width="150" Margin="0 0 10 0" Click="BtnStart_Click"/>
            <Button Content="⏹ İzlemeyi Durdur" Width="150" Click="BtnStop_Click"/>
        </StackPanel>

        <ListView x:Name="LogList" Height="350">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Zaman" Width="100" DisplayMemberBinding="{Binding Time}"/>
                    <GridViewColumn Header="Olay Türü" Width="150" DisplayMemberBinding="{Binding ChangeType}"/>
                    <GridViewColumn Header="Dosya" Width="400" DisplayMemberBinding="{Binding FileName}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </StackPanel>
</UserControl>
