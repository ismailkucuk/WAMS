<UserControl x:Class="wam.Pages.StartupProgramsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <StackPanel>
            <TextBlock Text="🚀 Startup Programs" FontSize="24"
                       FontWeight="Bold" Foreground="#2D2D30"
                       Margin="20,10,20,10"/>

            <ListView Name="StartupListView" Margin="20">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Program Adı" Width="200" DisplayMemberBinding="{Binding Name}" />
                        <GridViewColumn Header="Yol" Width="500" DisplayMemberBinding="{Binding Path}" />
                        <GridViewColumn Header="Kaynak" Width="150" DisplayMemberBinding="{Binding Source}" />
                    </GridView>
                </ListView.View>
            </ListView>
        </StackPanel>
    </Grid>
</UserControl>
