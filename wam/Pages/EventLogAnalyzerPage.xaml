<UserControl x:Class="wam.Pages.EventLogAnalyzerPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:controls="clr-namespace:wam.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000"
             Background="{DynamicResource BackgroundPage}">

    <UserControl.Resources>

        <Style x:Key="InfoCard" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource BackgroundCard}"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="25"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect ShadowDepth="1" Direction="270" Color="Black" Opacity="0.08" BlurRadius="12"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FilterRadioButton" TargetType="RadioButton">
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextColorSecondary}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="16">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource ControlHoverColor}"/>
                </Trigger>
                <Trigger Property="IsChecked" Value="True">
                    <Setter Property="Background" Value="{DynamicResource AccentColor}"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource AccentColor}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CustomListViewItem" TargetType="ListViewItem">
            <Setter Property="Cursor" Value="Hand"/>
            <EventSetter Event="MouseLeftButtonUp" Handler="ListViewItem_MouseLeftButtonUp"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <Border x:Name="Bd" Background="Transparent" Padding="{TemplateBinding Padding}">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ControlHoverColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- EXPORT CONTROL -->
        <controls:ExportControl Grid.Row="0" 
                               x:Name="ExportControl"
                               Margin="0,0,0,20"/>

        <Border Grid.Row="1" Style="{StaticResource InfoCard}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="5,0,5,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <!-- Filters left-aligned -->
                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                        <RadioButton Content="Uygulama" Style="{StaticResource FilterRadioButton}" IsChecked="True" Tag="Application" Checked="LogType_Checked"/>
                        <RadioButton Content="Sistem" Tag="System" Style="{StaticResource FilterRadioButton}" Checked="LogType_Checked"/>
                        <RadioButton Content="Güvenlik" Tag="Security" Style="{StaticResource FilterRadioButton}" Checked="LogType_Checked"/>
                    </StackPanel>
                </Grid>

                <ListView Grid.Row="1" x:Name="EventLogListView" BorderThickness="0" HorizontalContentAlignment="Stretch"
                          ItemsSource="{Binding EventLogEntries}"
                          ItemContainerStyle="{StaticResource CustomListViewItem}"
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                          PreviewMouseWheel="EventLogListView_PreviewMouseWheel">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border Name="ItemBorder" BorderThickness="4,0,0,1" Padding="6,12,10,12">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="BorderBrush" Value="{DynamicResource InfoColor}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding EntryType}" Value="Warning">
                                                <Setter Property="BorderBrush" Value="{DynamicResource WarningColor}"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding EntryType}" Value="Error">
                                                <Setter Property="BorderBrush" Value="{DynamicResource ErrorColor}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="150"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Liste ikonları geri getirildi ve kurumsal: entry tipine göre renkleniyor -->
                                    <TextBlock Grid.Column="0" VerticalAlignment="Center" Margin="0,0,20,0" FontFamily="Segoe MDL2 Assets" FontSize="20">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Text" Value="&#xE946;"/>
                                                <Setter Property="Foreground" Value="{DynamicResource InfoColor}"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding EntryType}" Value="Warning">
                                                        <Setter Property="Text" Value="&#xE7BA;"/>
                                                        <Setter Property="Foreground" Value="{DynamicResource WarningColor}"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding EntryType}" Value="Error">
                                                        <Setter Property="Text" Value="&#xE894;"/>
                                                        <Setter Property="Foreground" Value="{DynamicResource ErrorColor}"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>

                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Source}" FontWeight="Bold" TextTrimming="CharacterEllipsis"/>
                                        <TextBlock Text="{Binding Message}" Foreground="{DynamicResource TextColorSecondary}" TextTrimming="CharacterEllipsis" Margin="0,2,0,0" ToolTip="Detayları görmek için tıklayın"/>
                                    </StackPanel>

                                    <TextBlock Grid.Column="2" Text="{Binding TimeGeneratedFormatted}" Foreground="{DynamicResource TextColorSecondary}" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Border>
    </Grid>
</UserControl>