<UserControl x:Class="wam.Pages.UserActivityPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <StackPanel>
            <TextBlock Text="🧑‍💻 User Activity Monitor" FontSize="24"
                       FontWeight="Bold" Foreground="#2D2D30"
                       Margin="20,10,20,10"/>

            <ListView Name="ActivityListView" Margin="20">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Zaman" Width="200" DisplayMemberBinding="{Binding TimeCreated}" />
                        <GridViewColumn Header="Olay Türü" Width="100" DisplayMemberBinding="{Binding EventType}" />
                        <GridViewColumn Header="Kullanıcı Adı" Width="200" DisplayMemberBinding="{Binding UserName}" />
                    </GridView>
                </ListView.View>
            </ListView>
        </StackPanel>
    </Grid>
</UserControl>
