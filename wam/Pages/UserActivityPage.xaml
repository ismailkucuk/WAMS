<UserControl x:Class="wam.Pages.UserActivityPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:wam.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000"
             Background="{DynamicResource BackgroundPage}">

    <UserControl.Resources>

        <!-- Stiller -->
        <Style x:Key="InfoCard" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource BackgroundCard}"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="25"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect ShadowDepth="1" Direction="270" Color="Black" Opacity="0.08" BlurRadius="12"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="FilterRadioButton" TargetType="RadioButton">
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextColorSecondary}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="16">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource ControlHoverColor}"/>
                </Trigger>
                <Trigger Property="IsChecked" Value="True">
                    <Setter Property="Background" Value="{DynamicResource AccentColor}"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource AccentColor}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CustomListViewItem" TargetType="ListViewItem">
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Cursor" Value="Hand"/>
            <EventSetter Event="MouseLeftButtonUp" Handler="ListViewItem_MouseLeftButtonUp"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <Border x:Name="Bd" Background="Transparent" Padding="{TemplateBinding Padding}">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ControlHoverColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- EXPORT CONTROL -->
        <controls:ExportControl Grid.Row="0" x:Name="ExportControl" Margin="0,0,0,20"/>

        <Border Grid.Row="1" Style="{StaticResource InfoCard}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="5,0,5,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <!-- Filters left-aligned -->
                    <StackPanel Grid.Column="0" Orientation="Horizontal">
                        <RadioButton Content="{DynamicResource UserActivity_All}" Style="{StaticResource FilterRadioButton}" IsChecked="True" Tag="All" Checked="Filter_Checked"/>
                        <RadioButton Content="{DynamicResource UserActivity_LogonLogoff}" Tag="LogonLogoff" Style="{StaticResource FilterRadioButton}" Checked="Filter_Checked"/>
                        <RadioButton Content="{DynamicResource UserActivity_Critical}" Tag="Critical" Style="{StaticResource FilterRadioButton}" Checked="Filter_Checked"/>
                    </StackPanel>
                </Grid>

                <ListView Grid.Row="1" x:Name="ActivityListView" BorderThickness="0"
                          Background="{DynamicResource BackgroundCard}"
                          ItemsSource="{Binding UserActivities}"
                          ItemContainerStyle="{StaticResource CustomListViewItem}"
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                          PreviewMouseWheel="ActivityListView_PreviewMouseWheel">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="{DynamicResource BorderColor}" BorderThickness="0,0,0,1" Padding="10,12">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="200"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Liste ikonları geri getirildi ve kurumsal: event tipine göre renkleniyor -->
                                    <TextBlock Grid.Column="0" VerticalAlignment="Center" Margin="0,0,20,0" FontFamily="Segoe MDL2 Assets" FontSize="24">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Text" Value="&#xE77B;"/>
                                                <Setter Property="Foreground" Value="{DynamicResource TextColorSecondary}"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding EventId}" Value="4624">
                                                        <Setter Property="Text" Value="&#xE836;"/>
                                                        <Setter Property="Foreground" Value="{DynamicResource SuccessColor}"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding EventId}" Value="4634">
                                                        <Setter Property="Text" Value="&#xE777;"/>
                                                        <Setter Property="Foreground" Value="{DynamicResource DangerColor}"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding EventId}" Value="4672">
                                                        <Setter Property="Text" Value="&#xE72E;"/>
                                                        <Setter Property="Foreground" Value="{DynamicResource AccentColor}"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding EventId}" Value="4740">
                                                        <Setter Property="Text" Value="&#xE7F4;"/>
                                                        <Setter Property="Foreground" Value="{DynamicResource WarningColor}"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>

                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding UserName}" FontWeight="Bold" FontSize="15" Foreground="{DynamicResource TextColorPrimary}"/>
                                        <TextBlock Text="{Binding EventType}" FontSize="12" Foreground="{DynamicResource TextColorSecondary}" Margin="0,2,0,0"/>
                                    </StackPanel>

                                    <TextBlock Grid.Column="2" Text="{Binding TimeCreated}" Foreground="{DynamicResource TextColorSecondary}" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Border>
    </Grid>
</UserControl>
