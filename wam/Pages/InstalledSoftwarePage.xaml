<UserControl x:Class="wam.Pages.InstalledSoftwarePage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <StackPanel Margin="20">
            <TextBlock Text="Yüklü Yazılımlar Listesi" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>
            <Button Content="Listeyi Güncelle" Width="150" Click="FetchSoftwareList_Click" Margin="0,0,0,10"/>
            <DataGrid x:Name="SoftwareDataGrid" AutoGenerateColumns="False" Height="350">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Ad" Binding="{Binding Name}" Width="*"/>
                    <DataGridTextColumn Header="Yayıncı" Binding="{Binding Publisher}" Width="*"/>
                    <DataGridTextColumn Header="Sürüm" Binding="{Binding Version}" Width="*"/>
                    <DataGridTextColumn Header="Yüklenme Tarihi" Binding="{Binding InstallDate}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>
</UserControl>
