<Window x:Class="wam.Dialogs.CloseConfirmDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource Dialog_Close_Title}" Width="560" WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize" ShowInTaskbar="False" Background="{DynamicResource BackgroundPage}"
        SizeToContent="Height"
        Icon="pack://application:,,,/Resources/wams.ico">
    <Window.Resources>
        <Style x:Key="DialogPrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource AccentColor}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{DynamicResource PrimaryButtonHover}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bd" Property="Opacity" Value="0.9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DialogSecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource BackgroundWhite}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextColorPrimary}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{DynamicResource SecondaryButtonHover}"/>
                                <Setter TargetName="bd" Property="BorderBrush" Value="{DynamicResource AccentColor}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{DynamicResource SecondaryButtonPressed}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid Background="{DynamicResource BackgroundPage}">
        <Border Background="{DynamicResource BackgroundWhite}" CornerRadius="10" Padding="24" BorderBrush="{DynamicResource BorderColor}" BorderThickness="1" Margin="16">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="0" Color="{DynamicResource ShadowColor}" Opacity="0.10" BlurRadius="18"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="44"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Border Grid.Column="0" Width="36" Height="36" Background="{DynamicResource ChipBackground}" CornerRadius="18" HorizontalAlignment="Left" VerticalAlignment="Center">
                        <TextBlock Text="&#xE7E8;" FontFamily="Segoe MDL2 Assets" Foreground="{DynamicResource AccentColor}" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <TextBlock Grid.Column="1"
                               Text="{DynamicResource Dialog_Close_Message}"
                               TextWrapping="Wrap"
                               FontSize="15"
                               Foreground="{DynamicResource TextColorPrimary}"
                               Margin="12,0,0,0"/>
                </Grid>

                <StackPanel Grid.Row="1" Margin="0,16,0,16">
                    <RadioButton x:Name="MinimizeOption" Content="{DynamicResource Dialog_Close_Minimize}" IsChecked="True" FontSize="13" Foreground="{DynamicResource TextColorPrimary}"/>
                    <RadioButton x:Name="CloseOption" Content="{DynamicResource Common_Close}" FontSize="13" Foreground="{DynamicResource TextColorPrimary}"/>
                    <CheckBox x:Name="RememberOption" Content="{DynamicResource Dialog_Close_Remember}" Margin="0,12,0,0" FontSize="12" Foreground="{DynamicResource TextColorPrimary}"/>
                    <TextBlock Text="{DynamicResource Dialog_Close_RememberInfo}" FontSize="11" Foreground="{DynamicResource TextColorSecondary}" Margin="0,4,0,0"/>
                </StackPanel>

                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="{DynamicResource Common_Cancel}" Width="96" Margin="0,0,10,0" IsCancel="True" Style="{StaticResource DialogSecondaryButton}"/>
                    <Button Content="{DynamicResource Dialog_Close_Confirm}" Width="120" Click="Ok_Click" IsDefault="True" Style="{StaticResource DialogPrimaryButton}"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
